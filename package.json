{
  "name": "murshid",
  "version": "1.0.0",
  "description": "A lightening fast personal website",
  "main": "index.js",
  "private": true,
  "scripts": {
    "start": "open-cli http://localhost:1313 && hugo server --watch --disableFastRender",
    "clean": "rm -rf public/*",
    "build": "hugo --verbose"
  },
  "author": "Murshid Azher",
  "license": "MIT",
  "devDependencies": {
    "open-cli": "^6.0.1"
  }
}
