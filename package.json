{
  "name": "murshid",
  "version": "1.0.0",
  "description": "A simple personal website",
  "main": "index.js",
  "author": "Murshid Azher",
  "license": "ISC",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "hugo server --watch --disableFastRender",
    "clean": "rm -rf public",
    "build": "hugo --verbose",
    "format": "prettier --write \"themes/**/*.{html}\" \"assets/scss/**/*.{scss}\""
  },
  "dependencies": {
    "autoprefixer": "^9.7.1",
    "postcss-cli": "^6.1.3",
    "prettier": "^2.0.5"
  },
  "devDependencies": {
    "prettier-plugin-go-template": "0.0.4"
  }
}
